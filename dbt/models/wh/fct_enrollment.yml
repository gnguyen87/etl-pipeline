version: 2

models:
  - name: fct_enrollment
    description: Enrollment data for each school district by grade, race, and year per row
    columns:
      - name: k_school_district
        description: surrogate key of school district leaid (Local education agency identification number (NCES))
        tests:
          - not_null
 